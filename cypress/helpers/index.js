export { addProgramDimensions } from './addProgramDimensions.js'
export { choosePeriod, FIXED } from './choosePeriod.js'

const getPreviousYearStr = () => (new Date().getFullYear() - 1).toString()

export { getPreviousYearStr }
